
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export function StripeIntegration() {
  return (
    <div className="container mx-auto p-6">
      <h1 className="text-3xl font-bold mb-8">Stripe Integration</h1>
      <Card>
        <CardHeader>
          <CardTitle>Connect Stripe</CardTitle>
        </CardHeader>
        <CardContent>
          <p>Stripe integration interface will be implemented here.</p>
        </CardContent>
      </Card>
    </div>
  );
}
